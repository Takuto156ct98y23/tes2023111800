// import { getMe } from "../../api/apiUser";
// import { meSliceActions } from "./me-slice";

// export const getAndrenewMe = (me) => {
//   return async (dispatch) => {
// const me = await getMe(signal);

// dispatch(meSliceActions.renewMe(me));
//   };
// };
// export const getAndrenewMe = (signal = null) => {
//   return async (dispatch) => {
//     const me = await getMe(signal);

//     dispatch(meSliceActions.renewMe(me));
//   };
// };
